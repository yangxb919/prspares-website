# 2026-03-31 变更日志

## 操作记录

### 修改 - Batteries 产品页添加交互式 CTA
- **时间**: 2026-03-31
- **操作类型**: 修改
- **涉及文件**: `src/app/products/batteries/page.tsx`
- **变更说明**:
  - Product type 卡片（What We Supply）: `<div>` 改为 `<Link>` 跳转到 wholesale-inquiry，添加 "Get Quote" CTA 文案 + ArrowRight 图标 + hover 效果
  - Brand 卡片（Compatible Brands）: `<div>` 改为 `<Link>` 跳转到 wholesale-inquiry（含品牌名参数），hover 时显示 ArrowRight 图标
  - Quality grade 卡片（Quality Grades）: `<div>` 改为 `<Link>` 跳转到 wholesale-inquiry（含等级名参数），添加 "Request {grade} Pricing" CTA
  - 添加固定悬浮 Get Quote 按钮（fixed bottom-right）

### 修改 - Small Parts 产品页添加交互式 CTA
- **时间**: 2026-03-31
- **操作类型**: 修改
- **涉及文件**: `src/app/products/small-parts/page.tsx`
- **变更说明**:
  - Product type 卡片（What We Supply）: `<div>` 改为 `<Link>` 跳转到 `/wholesale-inquiry?product=Small+Parts`，添加 "Get Quote" CTA + ArrowRight 图标 + hover 效果
  - Brand 卡片（Compatible Brands）: `<div>` 改为 `<Link>` 跳转到 `/wholesale-inquiry?product={brand}+Parts`，hover 时显示 ArrowRight 图标
  - Part category 卡片（Full Parts Catalog）: `<div>` 改为 `<Link>` 跳转到 `/wholesale-inquiry?product={category}`，添加 "Get Quote" CTA
  - 添加固定悬浮 Get Quote 按钮（fixed bottom-right）

### 修改 - 基于 Clarity 数据的全站优化（5项）
- **时间**: 2026-03-31
- **操作类型**: 修改
- **涉及文件**:
  - `src/lib/analytics.ts`
  - `public/lp/google-ads-factory-direct.html`
  - `src/app/products/screens/page.tsx`
  - `src/app/page.tsx`
  - `src/app/wholesale-inquiry/page.tsx`
- **变更说明**:
  1. **P0 UTM 跟踪修复** (`analytics.ts`): 添加 `captureUtmParams()` / `getUtmParams()` 函数，从 URL 解析 UTM 参数并存入 sessionStorage，`trackEvent()` 自动附带 UTM 到所有 GA4 事件
  2. **P0 广告落地页首屏重构** (`google-ads-factory-direct.html`): Hero 从文字+视频改为产品卡片优先布局（Screens/Batteries/Parts 三张卡片），双 CTA（Request Quote + WhatsApp），添加 UTM 解析并附带到表单提交和 GA4 事件
  3. **P1 产品页交互元素** (`screens/page.tsx`, `batteries/page.tsx`, `small-parts/page.tsx`): 产品类型/品牌/质量等级卡片从纯展示 `<div>` 改为可点击 `<Link>`（跳转到 wholesale-inquiry 并预填参数），添加浮动 Get Quote 按钮
  4. **P2 首页优化** (`page.tsx`): 产品分类上移紧接 Hero，新增 Social Proof 数据条（10+ Years / 1000+ Clients / 50+ Countries / <1% RMA），Who We Serve 下移
  5. **P2 询价表单简化** (`wholesale-inquiry/page.tsx`): 必填字段从 4 个减至 3 个（email + 产品 + 数量），其余字段折叠到 "More Details (Optional)" 可展开区域，添加 WhatsApp 替代 CTA，支持 URL 参数预填产品类别
