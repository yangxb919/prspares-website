# 2026-04-16 变更记录

## 16:00-16:20 · 安装并接入 mcp-memory-service（跨项目语义记忆层）

**操作类型**: 配置 / 新增（全局 Claude Code 基础设施，与本项目代码无关）

**涉及路径**:
- `/Users/yangxiaobo/.claude/memory-service/`（新建隔离目录，venv + Python 3.12.12）
- `/Users/yangxiaobo/.claude/memory-service/memories.db`（SQLite-vec 向量库，1.6 MB）
- `~/.claude.json`（通过 `claude mcp add` 自动写入 `memory-service` MCP server 配置）
- `~/.cache/huggingface/hub/models--sentence-transformers--all-MiniLM-L6-v2/`（预热 embedding 模型）

**变更说明**:
1. 用 `uv venv --python 3.12` 在 `~/.claude/memory-service/venv/` 创建独立 Python 环境，避免污染当前项目目录
2. `uv pip install mcp-memory-service`（含 sqlite-vec 0.1.9、sentence-transformers 5.4.1、torch 2.11.0）
3. 通过 `claude mcp add memory-service ... -s user` 注册全局 MCP server，环境变量：
   - `MCP_MEMORY_STORAGE_BACKEND=sqlite_vec`
   - `MCP_MEMORY_SQLITE_PATH=/Users/yangxiaobo/.claude/memory-service/memories.db`
4. 首次启动 HF 模型缓存损坏（下载中断），删除残片并用 Python 脚本重新完整下载（~102s）
5. 端到端验证：直接调用 `SqliteVecMemoryStorage.store/retrieve`，写一条 PRSPARES 业务描述，英文问句召回相似度 0.797、耗时 250ms
6. `claude mcp list` 显示 `memory-service: ✓ Connected`

**特性配置**:
- ✅ 跨项目记忆：单库架构，通过 `project:xxx` tag 区分项目
- ✅ sqlite_vec 本地后端（纯本地，无云同步）
- ✅ auto-capture hook 默认开启
- ❌ session-harvest LLM 分类关闭（避免 Groq API key 成本）
- ❌ Cloudflare 云后端关闭

**遗留事项**:
- 本对话启动时 MCP 工具列表已固定，`mcp__memory-service__*` 系列工具要**下次启动 Claude Code** 才会出现
- `memory status` CLI 有小 bug（`got an unexpected keyword argument 'storage_backend'`），不影响 MCP 使用

---

## 今日晚间 · 修复 GSC 404 索引问题

**操作类型**: 修复

**涉及文件**:
- `next.config.js`

**变更说明**:
经 Google Search Console 排查，网站共有 15 条 404 URL 导致验证失败（验证失败期：2026/4/9–4/14）。逐一检测后发现：
- 14 条 URL 的重定向已存在且实际返回 200，无需修改
- 仅 1 条真正缺失重定向：`/blog/replacement-parts-repeat-orders-repair-shops`

新增重定向规则：
```
/blog/replacement-parts-repeat-orders-repair-shops → /blog (301)
```

**下一步**: 部署后在 GSC 手动点击「重新验证修复」
