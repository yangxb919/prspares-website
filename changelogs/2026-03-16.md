# 2026-03-16 操作日志

## 部署稳定性检查 & SOP 文档生成

- **时间**: 2026-03-16
- **操作类型**: 检查 / 新增
- **涉及文件**:
  - `package.json`（检查，未修改）
  - `next.config.js`（检查，未修改）
  - `ecosystem.config.cjs`（检查，未修改）
  - `.github/workflows/deploy.yml`（检查，未修改）
  - `.github/workflows/deploy-debug.yml`（检查，未修改）
  - `.env.local`（检查 keys，未修改）
  - `.env.example`（检查，未修改）
  - `DEPLOY_SOP.md`（**新增**）
- **变更说明**:
  1. 完成依赖完整性检查：确认 `rehype-raw`、`remark-gfm`、`react-markdown` 均在 dependencies 中
  2. 执行本地 `npm run build:fast` 构建验证，确认构建成功
  3. 检查部署相关配置文件（PM2、GitHub Actions、环境变量、next.config）
  4. 生成 `DEPLOY_SOP.md` 部署 SOP 文档，包含：部署前检查清单、标准部署步骤（自动/手动）、常见故障排查、回滚方案、部署后验收清单
