# 2026-03-14

## 00:40 — 性能优化 — Google Ads Landing Page

- **操作类型**: 修改 / 新增
- **涉及文件**:
  - `public/lp/google-ads-factory-direct.html`（修改）
  - `public/images/wholesale/vid_04_poster.jpg`（新增，17KB）
- **变更说明**:
  1. 为 hero 视频 vid_04.mp4 生成专属 poster 图（17KB），替换错误引用的 vid_02_poster
  2. 添加 `<link rel="preload">` 预加载 hero poster，加速首屏渲染
  3. 添加 unpkg.com preconnect 提示
  4. Lucide 图标库版本从 `@latest`（无缓存）固定为 `@0.468.0`（可缓存）
  5. 仓库展示区 3 个视频从 autoplay 改为 IntersectionObserver 懒加载（preload="none" + 滚动到视口 200px 时才加载播放），节省首屏约 1MB 下载
  6. Hero 视频添加 `preload="auto"` 明确指示浏览器尽早缓冲

## — 表单提交后跳转 Thank You 页面

- **操作类型**: 新增 / 修改
- **涉及文件**:
  - `public/lp/thank-you.html`（新增）
  - `public/lp/google-ads-factory-direct.html`（修改）
- **变更说明**:
  1. 新增 `/lp/thank-you.html` 感谢页面，包含成功确认、后续流程说明（Review → Quote → Ship）、WhatsApp 快速联系和产品浏览入口，保持 GTM 容器代码和品牌视觉一致
  2. 修改 landing page 表单提交成功逻辑：GA4 `generate_lead` 事件触发后通过 `eventCallback` 跳转至 thank-you 页面，同时设置 2.5s fallback `setTimeout` 确保即使 GTM 回调未触发也能正常跳转
  3. 移除原有的 inline 成功消息和 4s 后重置按钮的逻辑
