# 2026-08-03 操作日志

## RFQ `generate_lead` 补记与自动回归审计

**操作类型**：代码安全并入（未 Push、Deploy，未调用生产 API）

### 生产修复

以下 4 个 RFQ 提交入口已在 `await submitRfqAndNotify(...)` 成功返回后、成功跳转或状态更新前，各补记且仅补记一次：
`trackEvent('generate_lead', { currency: 'USD', value: 100 })`。

- `src/components/InquiryModal.tsx`
- `src/components/QuoteModalEnhanced.tsx`
- `src/components/ContactFormEnhanced.tsx`
- `src/components/features/ContactForm.tsx`

另外 4 个原本已正确的 RFQ 入口保持不变。

### 自动审计

- 新增 `scripts/tracking-contract.mjs`：自动发现 `src/**/*.ts`、`src/**/*.tsx` 中真实调用 `submitRfqAndNotify(...)` 的文件，并校验“恰好 1 次 awaited submit、恰好 1 次 `generate_lead`、submit 在 lead 之前”。
- 新增 `scripts/audit-tracking.mjs`：只读输出逐文件 PASS/FAIL 与汇总。
- 新增 `scripts/tracking-contract.test.mjs`：覆盖异常 fixture、精确发现 8 个真实入口和真实文件集成测试。
- `package.json` 新增 `audit:tracking` 与 `test:tracking`；未新增依赖、未修改 lockfile。
- 审计器为 dependency-free 的文本/正则检查，不是 TypeScript AST parser；相关限制已写入checker文件头。

### 实际验证

- `npm run test:tracking`：16 tests / 16 pass / 0 fail。
- `npm run audit:tracking`：`8 discovered, 8 passed`。
- 3 个新增 `.mjs` 文件 `node --check`：全部 exit 0。
- `npm run type-check`：exit 0。
- 基于最新提交代码的隔离集成验证：`next build` exit 0，223/223 静态页面生成成功；`next-sitemap` exit 0。
- 指定目标文件 `git diff --check`：exit 0。
