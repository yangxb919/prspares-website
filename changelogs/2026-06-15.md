# 2026-06-15 操作日志

## 15:43 — 修复 + 配置：GSC API 恢复 + 拉取近 28 天搜索效果

**操作类型**：修复 / 配置 / 排查

**触发**：用户收到 GSC 邮件「在 phonerepairspares.com 中检测到 1 个产品摘要结构化数据问题：应指定 offers / review / aggregateRating」，要求用 API 查看近期 GSC 情况。

**涉及文件**：
- `Analytics/scripts/gsc_fetch.py`（修改：refresh token 失效时不再崩溃，改为兜底浏览器重授权）
- `.secrets/gsc-token.json`（重新授权，新 refresh token；旧的备份为 `gsc-token.json.expired-bak-20260615`）
- `Analytics/gsc/2026-05-17_to_2026-06-13/`（新增：daily/queries/pages/devices/countries CSV + search_analytics.json）
- `src/app/products/[slug]/page.tsx`（仅排查，未改）

**变更说明**：
1. **GSC token 过期根因**：OAuth 同意屏处于「测试 / Testing」状态 → refresh token 每 7 天必失效（6-6→6-12→6-15 连续死亡）。治本要把 OAuth app 发布到「生产 / Production」。
2. **脚本 bug 修复**：`load_credentials()` 在 refresh token 被吊销时 `creds.refresh()` 直接抛 `RefreshError` 崩溃，走不到浏览器兜底。已用 try/except 包裹，失败时置 `creds=None` 自动转浏览器重授权。
3. **近 28 天数据（05-17→06-13）**：点击 57、展示 11,412、CTR 0.50%、均位 10.8。流量主要来自 blog（back-glass/boot-loop/screen-replacement 几篇），产品页几乎无点击。USA 展示最多但 CTR 仅 0.21%。
4. **邮件结构化数据根因（已坐实+已修）**：初判 `[slug]` 页被推翻——products 表仅 2 行(1 publish 含价 + 1 draft)。真凶 = `src/app/wholesale-inquiry/layout.tsx`：`Service.hasOfferCatalog` 里 5 个裸 `Product`(仅 name，无 offers/review/rating)。**GSC URL Inspection API 确认** `/wholesale-inquiry` indexed=PASS 但 richResults=FAIL，5 个分类名各报一条 ERROR。

## 16:00 — 修复：wholesale-inquiry 结构化数据报错

**操作类型**：修复
**涉及文件**：`src/app/wholesale-inquiry/layout.tsx`
**变更**：`hasOfferCatalog.itemListElement[].itemOffered` 的 `@type` 由 `Product` 改为 `Service`（5 处）。这些是 B2B 报价 Service 的品类桶、非带价 SKU；Service 不受 Google Product 校验「需 offers/review/aggregateRating」约束，且无需造假价格/评分。改后页面 JSON-LD = WebPage + Service + BreadcrumbList + FAQPage，无裸 Product。
**待办**：① 部署到 VPS（`ssh prspares`，注意 1GB 内存 build 约束）；② GSC「产品摘要」增强报告点「验证修复」或对 `/wholesale-inquiry` 请求重新编入索引。i18n 页 `/id/wholesale`、`/th/wholesale` 无此 schema 不受影响。
