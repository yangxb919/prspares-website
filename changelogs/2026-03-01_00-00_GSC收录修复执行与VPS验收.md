# 2026-03-01 00:00 - GSC收录修复执行与VPS验收记录

## 一、会话目标
围绕 `phonerepairspares.com` 的 GSC 不收录问题，完成诊断方案评审、优先级确定，并按用户确认的清单执行修复（先做 1-9 条，不执行第 10 条），最终部署到 VPS（`ssh prspares`）并完成线上验收。

## 二、关键讨论结论

### 1. 优先级判断
先处理“索引基础设施问题”（域名统一、重定向、canonical、404 修复、robots/noindex），再做内容优化。

原因：
- 若基础信号未统一，Google 仍会把抓取预算浪费在重复 URL / 错误 URL 上；
- 先投内容优化，效果会被 canonical/重定向冲突抵消；
- 先修“抓取与索引路径”，再提交流程更稳定。

### 2. 主域选择
本次按用户最终确认采用：
- 主域：`https://www.phonerepairspares.com`
- 非 www（`https://phonerepairspares.com`）统一 301 到 www。

## 三、执行范围（按清单 1-9）

### 已执行 1：统一 www/非www + 301
- 配置了 host/protocol 重定向，确保 apex 与 http 统一到 `https://www`。
- 同时对 SEO 相关默认站点 URL 进行了 www 统一。

### 已执行 2：修复 HTTP -> HTTPS
- `http://phonerepairspares.com`、`http://www.phonerepairspares.com` 均可最终到 `https://www.phonerepairspares.com`。

### 已执行 3：恢复 `/privacy-policy/`
- 新增隐私政策页面并在 footer 增加入口。

### 已执行 4：旧 `/product-category/` 与 `/product/` URL 跳转
- 旧分类与旧产品路由已做永久重定向；
- 包括已知拼写错误路径（`reapir`）的兼容跳转。

### 已执行 5：robots 屏蔽 `/_next/static/`
- `robots.txt` 增加：`Disallow: /_next/static/`。

### 已执行 6：博客标签/分类页 noindex
- `/blog?tag=...` 与 `/blog?category=...` 输出 `noindex, follow`。
- 同时 canonical 统一指向 `/blog`。

### 已执行 7：不相关文章处理检查
- 对“injection molding”等不相关内容做了数据库检索，未发现匹配发布文章（0 条可处理对象）。

### 已执行 8：SEO 元数据与 canonical 统一
- 博客详情页 canonical / og:url 统一为 www 域；
- 清理并归一化历史域名值（含 DB meta）；
- 过滤无效 `blob:` 图片元数据，补默认 `og:image/twitter:image` 回退图。

### 已执行 9：部署到 VPS 并验收
- 通过 `ssh prspares` 部署；
- 完成 build + PM2 restart；
- 逐项验证重定向、robots、sitemap、隐私页、旧 URL 跳转、博客 noindex/canonical。

## 四、过程中出现的问题与处理

### 1. `ssh prepares` 不可用
- 用户确认改用 `ssh prspares`，后续以该连接执行。

### 2. 部署过程中 rsync 误操作导致远端 `src` 不完整
- 症状：构建报 `Module not found`，远端 `src` 文件数降到 28。
- 处理：立即停止增量覆盖逻辑，改为本地项目全量重新同步到 `/var/www/prspares-website`，恢复至 112 个文件后重新构建与重启。

### 3. 本地 sitemap 一度出现 `localhost:3000`
- 原因：本地环境变量触发生成值不一致。
- 处理：重新按 www 域生成并核对线上结果，线上 sitemap 已为 www。

## 五、主要代码与配置变更（摘要）
- `next.config.js`：新增域名/协议统一与旧 URL 跳转规则。
- `next-sitemap.config.js`：站点 URL 默认改为 www，清理失效静态路径。
- `public/robots.txt`：屏蔽 `/_next/static/`，sitemap 指向 www。
- `src/app/privacy-policy/page.tsx`：新增隐私政策页面。
- `src/components/layout/Footer.tsx`：新增隐私政策链接。
- `src/app/blog/page.tsx`：tag/category 场景下 robots noindex。
- `src/app/blog/[slug]/page.tsx`：canonical/OG/Twitter URL 与图片归一化（含无效图片回退）。
- `src/components/admin/SEOEditor.tsx`、`src/components/admin/SEOPreview.tsx`：预览域名改为 www。

## 六、线上验收结果（最终）
- 域名与协议统一：通过。
- 隐私政策页面：可访问。
- 旧分类/产品/blog slug：均可跳转到新目标 URL。
- robots/sitemap：www 域一致。
- 博客 archive 页：`noindex, follow` 生效。
- 博客详情 canonical/og:url/twitter:image：www 域一致，且图片字段有效。

## 七、未执行项
按用户要求：
- 第 10 条（GSC 侧提交验证/再抓取等动作）未在本次执行。

## 八、当前状态
- 1-9 条已完成并上线验收通过；
- 网站当前主域策略为 `https://www.phonerepairspares.com`；
- 可进入下一步：在 GSC 发起“验证修正情况/请求重新抓取”（需用户确认后执行）。
