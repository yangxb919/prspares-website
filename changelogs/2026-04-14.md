# 2026-04-14 变更日志

## 17:30 — 新增 / 修改：博客与首页图片增加 Lightbox 放大功能
- **操作类型**: 新增 / 修改
- **涉及文件**:
  - `package.json`（新增依赖 `yet-another-react-lightbox`）
  - `src/components/LightboxImage.tsx`（新文件，封装 `LightboxImage`（next/image 版）+ `BlogLightboxImage`（原生 img 版））
  - `src/components/MarkdownRenderer.tsx`（博客正文 `img` 渲染替换为 `BlogLightboxImage`，保留原 normalize / onError 逻辑）
  - `src/app/page.tsx`（首页两张展示图：「OEM quality phone repair parts wholesale collection」与「PRSPARES warehouse and quality inspection」替换为 `LightboxImage`）
- **变更说明**: 根据 Microsoft Clarity（2026-04-12 ~ 2026-04-14）监测，博客 `iphone-screen-replacement-cost-guide` 与首页首屏多次出现 dead click（博客一会话 34 次、首页 12 次），原因是 Cloudinary 示意图 / 首页展示图未被链接或点击处理器包裹，用户点击想放大却无响应。本次接入轻量级 `yet-another-react-lightbox`（含 Zoom 插件），点击图片打开 lightbox，支持 ESC / 点击遮罩关闭、移动端捏合缩放；缩略图加 `cursor: zoom-in` + `role="button"` + `aria-label="Click to enlarge: {alt}"` 提升可发现性与可访问性。Cloudinary URL 在 lightbox 大图阶段替换 / 注入 `w_1600` 提升清晰度。未影响头部 logo、相关文章卡片图、产品品类 hero 图（这些本就是导航链接）。已通过 `npm run type-check` 与 `npm run build:fast`，零报错。
- **后续观察**: 等待次日 Clarity 数据中三个页面 dead click subTotal 是否显著下降。

## 16:39 — 脚本修复 / 数据同步
- **操作类型**: 修改 / 新增
- **涉及文件**: `Analytics/scripts/ga4_fetch_daily.py`, `Analytics/daily/2026-04-13.md`
- **变更说明**: 按 GA4 每日同步流程检查到 `Analytics/daily/` 最新已有日期为 `2026-04-12`，因此补齐缺失的 `2026-04-13`。同步过程中先后遇到两个阻塞并已修复：1）`ga4_fetch_daily.py` 中 `traffic_quality` 默认 YAML 写法触发 `f-string expression part cannot include a backslash`，已改为预先拼接默认块；2）GA4 属性当前不支持 `customEvent:traffic_quality_label` 维度，已将该查询改为降级容错，不再阻断整份日报生成。本次同步成功写入 `2026-04-13` daily。异常检查结果：Singapore 活跃用户 `64/92`（69.6%），需关注异常流量；`active_users` 较前一天 `72 -> 92`，波动未超过 50%。今日不是周一，未执行 weekly。

## GA4 数据检查
- **时间**: 下午
- **操作类型**: 检查
- **涉及文件**: 无代码变更（浏览器操作）
- **变更说明**: 
  - 通过 web-access CDP 登录 GA4 后台 (Property 502760218) 检查事件追踪和转化配置
  - 确认 19 个事件正确追踪，其中 9 个已标记为关键事件
  - 确认 GA4 已于 2026-03-29 与 Google Ads 账户 164-200-5192 成功关联
  - 发现 `generate_lead` 和 `thank_you_page_view` 事件有数据但未标记为关键事件，建议标记

## GA4 关键事件标记
- **时间**: 下午
- **操作类型**: 配置
- **涉及文件**: 无代码变更（GA4 后台操作）
- **变更说明**: 
  - 在 GA4 Admin > Data display > Events 中，将 `generate_lead` 和 `thank_you_page_view` 标记为关键事件（Key events）
  - 关键事件总数从 9 个增加到 11 个
  - 标记后这两个事件将出现在 GA4 关键事件报告中，并可通过 Google Ads 关联同步到广告后台
