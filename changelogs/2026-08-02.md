# 2026-08-02 操作日志

## 上午｜修复 + 新增｜两件技术类（08-07 窗口内允许做的 hygiene）

按 08-01 定的窗口期约束——只做不碰 cohort 那 20 个 URL 内容的技术改动，避免污染 08-07 判据。

### 一、2 条 404 → 301（Codex 建议 #3 的前半）

**涉及文件**：`next.config.js`（redirects 末尾新增一块）

| 404 URL | 301 目标 | 文章 |
|---|---|---|
| `/charging-port-failures-after-replacement` | `/blog/charging-port-failures-after-replacement` | id170, publish |
| `/iphone-unknown-part-warning-screen-replacement-ic-transfer` | `/blog/iphone-unknown-part-warning-...` | id147, publish |

两条都是**缺 `/blog` 前缀**，文章本身都在。grep 过 `src/`：站内没有任何链接指向这两个无前缀 URL
→ 来源是外部链接或历史索引 → **有外链权重正打在 404 上**，301 把它收回来。
Googlebot 分别在 07-22 / 04-30 抓过这两条。

校验：`node -e` 解析 redirects 确认新增 2 条、总数 41；`npm run build` 223 页通过。

### 二、Googlebot 抓取预算分桶脚本（Codex 建议 #2）

**新增** `Analytics/scripts/gbot_buckets.py`，并接入每日任务（`cohort_daily.sh` 里加跑 `--days 7`）。

与 cohort 是**两条独立证据线**：cohort 看 20 个 URL 的状态迁移，分桶看**全站预算花在哪**。

首次全量结果（1260 次 Googlebot 请求）：

| 桶 | 次数 | 占比 |
|---|---|---|
| canonical-blog | 355 | 28.2% |
| static | 307 | 24.4% |
| robots·sitemap | 272 | 21.6% |
| redirect | 114 | 9.0% |
| other | 109 | 8.7% |
| product | 46 | 3.7% |
| 404 | 31 | 2.5% |
| tag·query | 26 | 2.1% |

**🔴 真正花在内容页上的只有 32%（blog 28.2% + product 3.7%）**，其余 68% 是静态资源、
robots/sitemap、重定向。站点有 143 篇文章 + 数十产品页 ≈ 200 个内容页，
按 400 次/两周算，**平均每个内容页两周才被抓 2 次** —— 预算根本不够覆盖全站。
这比「内链不够」更能解释为什么新内容进不去索引。

### 三、⚠️ 一个需要更多数据的警报信号

`rt=` 字段显示 **p50=0.100s，p95=2.090s，42.9% 的请求 >1s**。
如果成立，Gemini 07-31 提的「1GB VPS 抓取容量受限」假设就不能排除
（当时我用「ChatGPT 抓 5259 次都扛住了」间接反驳过）。

**但样本只有 49 条**（`rt=` 字段 08-01 才加），且可能混入 Next.js SSR 首次渲染的慢请求。
**暂不下结论**，等日志积累几天后复看。这正是加 `rt=` 字段的价值——此前连怀疑都无法验证。
