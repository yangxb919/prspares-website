# 2026-06-05

## 15:30
- 操作类型：新增
- 涉及文件路径：
  - changelogs/2026-06-05.md
  - /Users/yangxiaobo/Documents/Obsidian Prspares/Sales/Daily Emails/2026-06-05 邮件起草日报.md（读取核验）
- 变更说明：检查 PRSPARES Zoho INBOX / Sent / Drafts，确认 2026-06-05 本地时间无今日新邮件、近 24 小时无待处理客户回复；历史 Draft UID 213 / 214 仍待 Bowen 审核发送。

## 16:08
- 操作类型：新增
- 涉及文件路径：
  - changelogs/2026-06-05.md
  - https://github.com/ZhuLinsen/daily_stock_analysis（只读分析，未克隆到本地）
- 变更说明：通过 GitHub API / raw 文件只读查看 daily_stock_analysis 项目 README、.env.example、部署文档、Docker 配置、GitHub Actions workflow 和核心源码，整理项目功能、部署方式与所需 API Key。

## 16:50
- 操作类型：新增
- 涉及文件路径：
  - changelogs/2026-06-05.md
  - https://github.com/ZhuLinsen/daily_stock_analysis/blob/main/.github/workflows/00-daily-analysis.yml（只读）
  - https://github.com/ZhuLinsen/daily_stock_analysis/blob/main/docs/full-guide.md（只读）
  - https://github.com/ZhuLinsen/daily_stock_analysis/blob/main/docs/deploy-webui-cloud.md（只读）
- 变更说明：核验 daily_stock_analysis 的 GitHub Actions、Docker、WebUI/FastAPI 部署说明，整理 GitHub 部署与服务器部署的功能差异。

## DataForSEO MCP 本项目重新禁用（保留全局配置/账号）

**时间**：2026-06-05
**操作类型**：配置
**涉及文件**：`~/.claude.json`（项目外全局配置，非 repo 内）

**变更说明**：本项目 DataForSEO MCP 又出现 83 个 tools 常驻加载。根因=用户级 MCP 的逐项目禁用列表 `projects['.../prspares-website'].disabledMcpServers` 里漏了 `dataforseo`（之前掉了项）。修复：把 `dataforseo` 加回该列表 → `['pencil','claude.ai Google Drive','exa','dataforseo']`。

- 全局 `mcpServers.dataforseo`（含凭据 wendy-lee@phonerepairspares.com）**未动**，其它项目仍可用 MCP。
- 需要 DataForSEO 时走 REST API / 本地脚本（凭据从 ~/.claude.json 读），不常驻 MCP tools。
- 备份：`~/.claude.json.bak-<mtime>`。
- ⚠️ 需重启 Claude Code 生效。

## 加固 REST 替代 DataForSEO MCP — 统一 CLI `Analytics/scripts/dfs.py`

**时间**：2026-06-05
**操作类型**：新增
**涉及文件**：`Analytics/scripts/dfs.py`（新建，~430 行）

**背景**：禁用 MCP 后核实——DataForSEO MCP server 就是 REST 的薄封装（每个工具一行 `makeRequest('/v3/...','POST',[{...}])`，base=api.dataforseo.com，84 个工具文件）。所以 REST 功能 = MCP 超集，绝不更差；MCP 仅胜在「即兴免写代码」的便利。固定流程下脚本化更优（省 context + 可缓存 + 可落盘）。

**变更说明**：把之前在 /tmp（已被清）的零散 dfs 脚本固化成一个统一 CLI：
- 子命令：`vol`(月搜量,带缓存) `overview` `serp`(SERP特征/AIO检测) `chatgpt`(GEO抓ChatGPT,--web) `llm`/`models` `ranked`/`ideas`/`suggestions`/`difficulty`/`intent`/`domain`，外加 `raw <path> --data @f.json` 兜底任意 /v3 端点（保证=MCP 全部工具超集）。
- 凭据从 ~/.claude.json 递归自取（不硬编码、不 commit）；`vol` 与 keyword_opportunity.py 共用 `.dfs_volume_cache.json` 省钱。
- 全局 `--json`/`--full`/`--out`；顶层+task 状态与 cost 打到 stderr。
- 已 live 冒烟测试通过：vol/serp(AIO检测正常)/overview/models/raw 均 status=20000，单次成本 $0.0035~0.075。

**配套**：更新记忆 `reference_citability_recipe_and_dataforseo.md` §2（含确切端点 + body 须数组）。
